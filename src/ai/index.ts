// ============================================================
// AI 集成层 — 统一导出
// ============================================================

export { buildPrompt, buildPromptShort } from './prompt-builder'
export { parseAIResponse } from './response-parser'
export { generateAIDecision } from './api'
export { AI_OUTPUT_SCHEMA, AI_OUTPUT_EXAMPLE } from './schema'
